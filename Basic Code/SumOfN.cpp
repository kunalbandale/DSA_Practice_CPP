#include<iostream>
 using namespace std;

int main() {
  int x , y;
  int sum = 0;

 cin >> x >> y ;

  for(int i = x ; i<=y;i++) {
    sum+=i;
  }
  cout << sum;
  
  

  using formula
  n = (n*(n+1))/2
  x = (x*(x+1)/2);
  cout << x;

  using loop
  for(int i = 1 ; i <= x ; i++) {
     sum = i + sum;
  }
  cout << sum << endl;

  
  return 0;
}